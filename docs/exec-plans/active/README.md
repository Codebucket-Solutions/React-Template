# Active plans

Add one markdown file per active non-trivial task.
Include owner and write scope when the task may involve more than one agent.
The active plan file is also the task claim for multi-agent work.
Use `npm run plan:new <slug>` to create a starter plan file.
